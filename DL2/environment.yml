# Conda environment
#
# Install:
# mamba env create -f environment.yml
# conda activate EDconvert

name: EDconvert
channels:
  - astropy
  - conda-forge

dependencies:
  - pre-commit
  - numpy
  - astropy
  - click
  - python
  - uproot=4.2.4
